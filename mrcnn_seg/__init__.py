"""Mask R-CNN (torchvision) para segmentação de instâncias.

Pacote utilitário com dataset (formato YOLO-seg), construção do modelo,
loop de treino e funções auxiliares compartilhadas pelos scripts
train.py / predict.py / validate.py / export.py.
"""
